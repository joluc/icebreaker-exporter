apiVersion: v1
kind: Service
metadata:
  name: {{ include "icebreaker-exporter.fullname" . }}
  labels:
    {{- include "icebreaker-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "icebreaker-exporter.selectorLabels" . | nindent 4 }}
